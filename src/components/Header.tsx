import React from 'react';

export const Header = () => {
    return (
        <header className="max-w-screen-2xl mx-auto w-full">
            <nav className="bg-amber-300">Nawigacja</nav>
        </header>
    );
}